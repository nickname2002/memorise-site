# Shared tester password gating downloads on the landing page and
# macos-install page. Copy this file to .env and set a real value —
# .env is gitignored so the password never ends up in the repo.
VITE_TEST_PHASE_PASSWORD=
